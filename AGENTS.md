# AGENTS.md

## 项目现状（先读这条）

- **纯设计阶段仓库**：截至 2026-09-09，仓库内没有任何代码、工程文件、构建脚本，也尚未 `git init`。不要假设存在构建/测试/lint 命令；不要凭空生成 Unity/UE 工程结构。
- **唯一设计事实来源**：[`Docs/GameDesignDoc_v1.0.md`](Docs/GameDesignDoc_v1.0.md)（《战国·兵者诡道》RTS 产品设计文档 v1.0）。所有玩法、数值、阵营、AI 设计以该文档为准；文档与口头说法冲突时信文档。
- **配套审校意见**：[`Docs/GameDesignDoc_v1.0_审校意见.md`](Docs/GameDesignDoc_v1.0_审校意见.md)（v1.0 评审结论"有条件通过"：5 项 P0 阻塞、9 项 P1、12 项 P2）。**审校意见是待决策的建议，不是已采纳的设计**——在用户拍板并产出 GDD v1.1 前，实现与引用仍以 v1.0 原文为准；涉及 P0 条目（引擎、双层时间模型、克制环、网络防作弊架构、情报可信度模型）的工作必须先确认用户决策。
- 文档与沟通语言为**中文**；代码、API、文件名保持英文/原样。

## 未决事项（动手写代码前必须向用户确认）

- **游戏引擎未定**：文档抬头写的是 "Unity 2022 LTS / Unreal Engine 5"，但 §5.1 明确建议 **Unity 2022 LTS（C#）**，且 §5.2 的 AI 伪代码是 C#（ML-Agents + 行为树，网络层 Mirror/Photon）。在用户拍板前，不要提交任一引擎的脚手架、资产或 `.cs/.cpp` 代码。

## 范围控制（防止过度构建）

- 文档 §9.2 定义的 **MVP 只做**：赵、秦两个阵营；3 个诡道技能（减灶、增灶、火攻）；1 张地图（滏口陉–野王–高都）；幕僚 AI 仅廉颇 + 赵括；1 个战役剧本。
- 七国全阵营、三十六计全技能、多人对战、战役编辑器均属路线图（§6）第二阶段以后，未经明确要求不要提前实现。
- 核心设计支柱是**信息不对称与欺骗**（侦查可信度、假情报、AI 信任度系统），而非传统 RTS 的堆兵平推；改动玩法时不得削弱这一核心循环（庙算 → 战役实时 → 战后叙事）。

## 设计约定（实现时需遵守的文档事实）

- 兵种循环克制（v1.0 原文，审校 P0-3 认为该环不合理并建议改为"枪→骑→弩→重步→枪"，未决）：轻骑 → 弩兵 → 重步 → 枪兵 → 轻骑（§2.4）。
- 诡道技能是**数据驱动**的（谋略点消耗 + 冷却，部分全局仅一次，如"焚粮断道"），新技能应走配置而非硬编码。
- AI 架构：情报模块 → 信任度系统 → 目标优先级矩阵 → 行为树 → 适应模块（§3.1）；信任度 0–1、阈值 0.3/0.7 等数值见 §5.2 伪代码，调整需同步文档。
- 存档：JSON 本地/Steam Cloud；历史事件以可回放"编年史"保存（§5.3）。
- 目标平台：PC（Steam）优先，主机/移动端为后续。

## 文档工作流

- 设计文档放 `Docs/`，文件名带版本号（如 `GameDesignDoc_v1.0.md`）；路线图条目用 Markdown checkbox 跟踪（见 §6）。
- 改动已实现的玩法/数值时，同步修订设计文档并升版本号；代码中的伪代码（C#）仅为示意，不代表最终实现。
